import 'package:flutter/material.dart';

class ClientAccountSetup extends StatelessWidget {
  const ClientAccountSetup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
